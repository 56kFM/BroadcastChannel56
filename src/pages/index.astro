---
import List from '../components/list.astro'
import { getChannelInfo } from '../lib/telegram'

const channel = await getChannelInfo(Astro, { limit: 1 })
---

<List channel={channel} isItem={true} after={false} paginationGap={true} />
